# CellCount uses no external API keys — cell counting runs locally via OpenCV.
# This file is a placeholder for optional frontend configuration.

# Override the backend URL the frontend talks to (default: http://localhost:8000).
# Copy this file to frontend/.env if you need to change it.
# VITE_API_BASE=http://localhost:8000
